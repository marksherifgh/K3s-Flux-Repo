apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
 name: letsencrypt-issuer
 namespace: cert-manager
spec:
 acme:
   server: https://acme-staging-v02.api.letsencrypt.org/directory
   email: msherif@synapse-analytics.io
   privateKeySecretRef:
     name: letsencrypt-secret
   solvers:
   - http01:
       ingress:
         class:  nginx